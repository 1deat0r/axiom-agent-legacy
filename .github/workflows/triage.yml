name: Issue triage

on:
  issues:
    types: [opened, closed, labeled, unlabeled]

permissions:
  contents: read
  issues: write

jobs:
  triage-open:
    runs-on: ubuntu-latest
    if: ${{ github.event.action != 'closed' && !github.event.issue.pull_request }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
      - name: Decide
        id: decide
        run: |
          gh issue view "${{ github.event.issue.number }}" --json labels,comments,state \
            | npx --yes tsx@4.23.1 packages/coding-agent/src/core/gh-tooling/triage-cli.ts "${{ github.event.action }}" \
            > /tmp/decision.json
          echo "action=$(jq -r .action /tmp/decision.json)" >> "$GITHUB_OUTPUT"
          echo "label=$(jq -r .label /tmp/decision.json)" >> "$GITHUB_OUTPUT"
          echo "has_comment=$(jq -r 'if .comment then "true" else "false" end' /tmp/decision.json)" >> "$GITHUB_OUTPUT"
      - name: Apply role label
        if: steps.decide.outputs.action == 'needs-triage'
        run: gh issue edit "${{ github.event.issue.number }}" --add-label "${{ steps.decide.outputs.label }}"
      - name: Post readiness contract
        if: (steps.decide.outputs.action == 'needs-triage' || steps.decide.outputs.action == 'remind') && steps.decide.outputs.has_comment == 'true'
        run: |
          jq -r .comment /tmp/decision.json \
            | gh issue comment "${{ github.event.issue.number }}" --body-file -
      - name: Post role-conflict note
        if: steps.decide.outputs.action == 'role-conflict'
        run: |
          jq -r .comment /tmp/decision.json \
            | gh issue comment "${{ github.event.issue.number }}" --body-file -

  close-check:
    runs-on: ubuntu-latest
    if: ${{ github.event.action == 'closed' && !github.event.issue.pull_request }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
      - name: Check audit comment
        id: check
        run: |
          gh issue view "${{ github.event.issue.number }}" --json comments \
            | npx --yes tsx@4.23.1 packages/coding-agent/src/core/gh-tooling/triage-close-cli.ts \
            > /tmp/close-decision.json
          echo "action=$(jq -r .action /tmp/close-decision.json)" >> "$GITHUB_OUTPUT"
      - name: Post close-ritual reminder
        if: steps.check.outputs.action == 'nudge'
        run: |
          jq -r .comment /tmp/close-decision.json \
            | gh issue comment "${{ github.event.issue.number }}" --body-file -
